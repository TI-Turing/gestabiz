/**
 * Alias de compatibilidad. El routing por rol se hace en App.tsx.
 * Admin → AdminDashboardScreen, Employee → EmployeeDashboardScreen, Client → ClientDashboardScreen
 */
export { default } from './employee/EmployeeDashboardScreen'
