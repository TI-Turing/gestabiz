name: Deploy Functions to Production

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/send-email-reminder/**'
      - 'supabase/functions/send-whatsapp-reminder/**'
      - 'supabase/functions/send-sms-reminder/**'
      - 'supabase/functions/send-phone-otp/**'
      - 'supabase/functions/verify-phone-otp/**'
      - 'package.json'

jobs:
  deploy-functions:
    name: Deploy Edge Functions to Production
    runs-on: ubuntu-latest
    environment: Production

    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Deploy send-email-reminder to Production
        run: npx supabase functions deploy send-email-reminder --project-ref ${{ vars.SUPABASE_PROJECT_REF }} --dns-resolver https
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.PROD_SUPABASE_SECRET }}
      
      - name: Deploy send-whatsapp-reminder to Production
        run: npx supabase functions deploy send-whatsapp-reminder --project-ref ${{ vars.SUPABASE_PROJECT_REF }} --dns-resolver https
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.PROD_SUPABASE_SECRET }}
      
      - name: Deploy send-sms-reminder to Production
        run: npx supabase functions deploy send-sms-reminder --project-ref ${{ vars.SUPABASE_PROJECT_REF }} --dns-resolver https
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.PROD_SUPABASE_SECRET }}

      - name: Deploy send-phone-otp to Production
        run: npx supabase functions deploy send-phone-otp --project-ref ${{ vars.SUPABASE_PROJECT_REF }} --dns-resolver https
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.PROD_SUPABASE_SECRET }}

      - name: Deploy verify-phone-otp to Production
        run: npx supabase functions deploy verify-phone-otp --project-ref ${{ vars.SUPABASE_PROJECT_REF }} --dns-resolver https
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.PROD_SUPABASE_SECRET }}
      
      - name: Verify deployments
        run: npx supabase functions list --project-ref ${{ vars.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.PROD_SUPABASE_SECRET }}
      
      - name: Deployment Success
        if: success()
        run: echo "All functions deployed successfully to Production"
      
      - name: Deployment Failed
        if: failure()
        run: echo "Deployment to Production failed. Check logs above." && exit 1
